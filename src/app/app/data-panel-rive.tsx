/**
 * The supplied translucent-window Rive file is retained in public/rive for
 * future use as a standalone interaction. It is intentionally not mounted
 * inside data cards: its authored window scene competes with live card
 * content and makes the dashboard look layered.
 */
export function DataPanelRive() {
  return null;
}
