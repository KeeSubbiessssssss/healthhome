/**
 * HealthHome's single source of truth for application tables.
 *
 * Add Drizzle table definitions here before creating any route or UI that
 * reads or writes new data, then generate and commit the SQL migration.
 */
export {};
